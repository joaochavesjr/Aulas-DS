Entrar no VS Code e abrir o terminal

* No terminal executar o seguinte comando:

    ```&"C:\Program Files\Python313\python.exe" -m venv testenv```

* Para ativar o ambiente, digite no terminal:

    ```testenv\Scripts\activate```

* Para instalar uma nova biblioteca digite, por exemplo, o comando:

    ```pip install colorama```

